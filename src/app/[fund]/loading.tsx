// Dashboard loading state — skeletons, never spinners (SPEC 11.4).

import { DashboardSkeleton } from "@/components/ui/Skeleton";

export default function DashboardLoading() {
  return <DashboardSkeleton />;
}
